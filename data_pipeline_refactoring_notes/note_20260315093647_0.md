---
id: note_20260315093647_0
title: 我们已经完成了数据模型层的重构,测试覆盖率达到85%。下一步...
type: action
tags: ["data_pipeline_refactoring","auto_generated"]
created_at: 2026-03-15T09:36:47.170Z
updated_at: 2026-03-15T09:36:47.170Z
---

# 我们已经完成了数据模型层的重构,测试覆盖率达到85%。下一步...

## 问题
我们已经完成了数据模型层的重构,测试覆盖率达到85%。下一步计划重构业务逻辑层。

## 分析
1. 结论  
下一步应**优先重构核心业务流程（OrderProcessing、InventoryAllocation、PricingEngine）的领域服务层，并采用契约测试（Consumer-Driven Contracts）保障上下游集成稳定性**。

2. 依据  
- 历史笔记 #2023-11-05：“业务逻辑层中 OrderProcessingService 被 7 个下游服务调用，且其接口变更曾导致支付网关超时事故（见 incident report IR-2023-089）”；  
- 历史笔记 #2024-02-18：“架构评审纪要明确要求：业务逻辑层重构必须先完成‘核心三域’（订单、库存、定价）的服务边界梳理与 OpenAPI 3.0 契约定义，作为后续并行开发准入门槛”；  
- 历史笔记 #2024-03-12：“测试策略文档 v2.1 指出：当前契约测试覆盖率仅 12%，而目标为重构期间提升至 ≥90%（对应 blocker #B-042）”。

3. 风险与假设  
- 风险：现有业务逻辑大量耦合 DAO 和 HTTP 客户端（如硬编码调用 /inventory/adjust），若未先解耦依赖，将阻碍单元测试隔离与契约测试落地；  
- 假设：团队已具备 Spring Cloud Contract 或 Pact 的基础实践能力（依据培训记录 #TR-2024-03-05，但尚未在生产模块应用）。

4. 下一步行动建议  
✅ 立即启动（1–2 人日）：  
- 基于 domain-event-mapping.xlsx（最新版，见 /docs/domain/2024Q2/）识别 OrderProcessing、InventoryAllocation、PricingEngine 的输入/输出事件与外部契约；  
- 输出三份 OpenAPI 3.0 契约草案（含 request/response schema、状态码、示例），提交至 api-contracts repo 的 `business-logic/v1-alpha` 分支；  
- 同步更新 blocker #B-042 状态为 “in-progress”，并关联 PR 模板（使用 ./templates/pr-contract-draft.md）。  

⚠️ 同步准备（同步开展，不阻塞上述动作）：  
- 为 PricingEngine 编写首个契约测试用例（消费者：PromotionService），验证 price-calculation-v1 接口兼容性 —— 此为 blocker #B-042 的验收标准之一。